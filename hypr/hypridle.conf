general {
	lock_cmd = pidof hyprlock || hyprlock
	before_sleep_cmd = loginctl lock-session    #Of course I want to lock before I suspend ;)
	after_sleep_cmd = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'
}

listener {
	timeout = 180
	on-timeout = brightnessctl -s set 10
	on-resume = brightnessctl -r
}

listener {
	timeout = 300
	on-timeout = sh -c 'cat /sys/class/power_supply/BAT*/status | grep -q Discharging && loginctl lock-session'
}

listener {
	timeout = 360
	on-timeout = hyprctl dispatch 'hl.dsp.dpms({ action = "disable" })'
	on-resume = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })' && brightnessctl -r
}

listener {
	timeout = 900
	on-timeout = sh -c 'cat /sys/class/power_supply/BAT*/status | grep -q Discharging && systemctl suspend'
}
